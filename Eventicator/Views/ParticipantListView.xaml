<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Eventicator.Views.ParticipantListView"
             Title="{Binding Title}">

    <RefreshView Command="{Binding RefreshCommand}"
                 IsRefreshing="{Binding IsBusy}">

        <CollectionView ItemsSource="{Binding Participants}">

            <CollectionView.EmptyView>
                <VerticalStackLayout Padding="24" Spacing="8">
                    <Label Text="Keine Teilnehmer vorhanden." FontAttributes="Bold"/>
                    <Label Text="Sobald du welche hinzufügst, tauchen sie hier auf." Opacity="0.7"/>
                </VerticalStackLayout>
            </CollectionView.EmptyView>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <SwipeView>

                        <SwipeView.RightItems>
                            <SwipeItems Mode="Execute">
                                <SwipeItem Text="Löschen"
                                           Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DeleteCommand}"
                                           CommandParameter="{Binding .}" />
                            </SwipeItems>
                        </SwipeView.RightItems>

                        <Grid Padding="14" RowDefinitions="Auto,Auto">
                            <HorizontalStackLayout Spacing="8">
                                <Label Text="{Binding FirstName}" FontAttributes="Bold"/>
                                <Label Text="{Binding LastName}" FontAttributes="Bold"/>
                            </HorizontalStackLayout>

                            <Label Grid.Row="1"
                                   Text="{Binding Email}"
                                   FontSize="12"
                                   Opacity="0.7"/>
                        </Grid>

                    </SwipeView>
                </DataTemplate>
            </CollectionView.ItemTemplate>

        </CollectionView>

    </RefreshView>
</ContentPage>