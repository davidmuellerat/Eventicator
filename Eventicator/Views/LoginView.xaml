<?xml version="1.0" encoding="utf-8" ?>
<ContentPage                                                                                                                            
       x:Class="Eventicator.Views.LoginView"                                                                                              
       xmlns="http://schemas.microsoft.com/dotnet/2021/maui"                                                                               
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"                                                                              
       xmlns:viewmodels="clr-namespace:Eventicator.ViewModels"                                                                            
       Title="Login">

    <ContentPage.BindingContext>
        <viewmodels:LoginViewModel/>
    </ContentPage.BindingContext>

    <VerticalStackLayout Padding="20" Spacing="12">

        <Label Text="Login" FontSize="24" FontAttributes="Bold"/>

        <Entry Placeholder="Email" Text="{Binding Email}" Keyboard="Email" />
        <Entry Placeholder="Passwort" Text="{Binding Password}" IsPassword="True" />

        <Button Text="Login" Command="{Binding LoginCommand}" />
        <Button Text="Registrieren" Command="{Binding OpenRegisterCommand}" />

    </VerticalStackLayout>
</ContentPage>   